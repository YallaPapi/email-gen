ZAD MANDATE: 08-02-2025

PROJECT: Scalable Email Generator
STATUS: CRITICAL FAILURE. All previous approaches have failed. The system is non-functional, user trust is zero.
OBJECTIVE: Achieve a flawlessly working, end-to-end system that generates multi-email sequences from a CSV upload via a web interface. No more excuses. No more "it works on my machine."

I. CORE DIRECTIVES (NON-NEGOTIABLE)

    STOP GUESSING. All previous work has been a chaotic cycle of patching and praying. From now on, every action will be deliberate, tested, and verified.

    SIMPLIFY OR DIE. The current Celery chord architecture is the source of 90% of the failures. It is too complex and unreliable for this use case. We will replace it with a simpler, more robust, sequential process until it is flawless. Parallelism is a luxury we have not earned.

    TEST-DRIVEN ACTION. No code will be considered "done" until it is covered by a repeatable, end-to-end test that simulates the exact user workflow. This means:

        Uploading a file via the web UI.

        Waiting for the job to complete.

        Downloading the result.

        Verifying the output.

    NO MORE HARDCODING. The AI prompts will be intelligent, not a giant, hardcoded if/else block. The AI must be given the context and the rules to figure out the industry specifics on its own.

II. IMMEDIATE ACTION PLAN: OPERATION PHOENIX

This is a ground-up rebuild of the core logic. We are salvaging the container setup but gutting the failed task orchestration.

PHASE 1: STABILIZE THE CORE (BACKEND LOGIC)

    [SUBTASK 1.1] RIP OUT THE CHORD.

        Action: Go into backend/tasks.py. Delete the entire chord-based workflow for sequence generation (process_spreadsheet_sequence_task).

        Replacement: Create a single, synchronous Celery task called generate_email_sequence_for_row. This task will take a single row of data.

        Logic: Inside this one task, it will perform the three OpenAI API calls sequentially:

            Call for Initial Email.

            Call for Follow-up 1.

            Call for Follow-up 2.

        Result: The task will return a dictionary containing the complete data for one row (original data + 3 generated emails). This eliminates all concurrency and callback failures.

    [SUBTASK 1.2] REBUILD THE MAIN DISPATCHER.

        Action: Create a new main task process_spreadsheet_task.

        Logic: This task will:

            Read the uploaded CSV file row by row.

            For each row, it will call generate_email_sequence_for_row.delay() to queue the synchronous task.

        Result: This creates a simple, linear queue of jobs. It will be slower, but it will be reliable. Speed is irrelevant if the system doesn't work.

    [SUBTASK 1.3] FIX THE PROMPT ENGINEERING.

        Action: Go to the prompt for Follow-up 1.

        Logic: Remove all hardcoded industry examples. The prompt must instruct the AI to act as an expert consultant.

        New Prompt Example:

        "You are an expert AI business consultant. Based on the following company data, identify the most pressing business challenge a company in the {industry} sector like {organization_name} would face. Then, propose a specific, high-value AI solution (from our services: AI Chatbots, Automated Lead Gen, Database Reactivation) that directly solves that challenge. Frame it as a unique, tangible benefit. Do not be generic."

        Result: This forces the AI to do the "thinking," making the output dynamic and intelligent, as originally requested.

PHASE 2: FIX THE USER INTERFACE (FRONTEND)

    [SUBTASK 2.1] UNIFY THE FRONTEND.

        Action: The current frontend is a mess of duplicate routes and file path issues.

        Logic:

            In backend/main.py, delete all @app.get("/") routes.

            Implement a single app.mount("/static", StaticFiles(directory="../frontend"), name="static") directive.

            Create a single @app.get("/") route that returns a FileResponse pointing to ../frontend/index.html.

        Result: A clean, standard, and reliable way to serve the static frontend files.

PHASE 3: END-TO-END VALIDATION

    [SUBTASK 3.1] CREATE THE "GAUNTLET" TEST SCRIPT.

        Action: Create a new Python script test_e2e.py.

        Logic: This script will use the requests library to:

            POST a real-world test CSV (real_test.csv) to the http://localhost:8000/upload endpoint.

            Poll the /status/{job_id} endpoint in a loop until the status is SUCCESS or FAILURE.

            If SUCCESS, GET the result file from /download/{job_id}.

            Load the resulting Excel file using pandas.

            Assert that the file contains the correct number of rows and that the initial_email, followup_1, and followup_2 columns are not empty.

        Result: This is the only test that matters. It must pass flawlessly before this project is presented again.

This is the only path forward. No deviation. No shortcuts. Execute this mandate precisely.